# Configuration Governance

## Rule
Configuration must be explicit, validated, and environment-scoped.

## Required
- precedence order
- secret vs non-secret separation
- startup validation
- no silent fallback for critical config
